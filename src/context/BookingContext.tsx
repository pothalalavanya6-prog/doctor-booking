import { createContext, PropsWithChildren, useContext, useState } from 'react';
import { Booking, Doctor, PatientDetails, PaymentMode, TimeSlot } from '../types';
interface BookingContextValue { selectedDoctor?: Doctor; selectedDate?: string; selectedSlot?: TimeSlot; patient?: PatientDetails; paymentMode?: PaymentMode; booking?: Booking; chooseDoctor: (doctor: Doctor) => void; chooseSlot: (date: string, slot: TimeSlot) => void; savePatient: (patient: PatientDetails) => void; choosePayment: (mode: PaymentMode) => void; confirmBooking: () => void; reset: () => void; }
const BookingContext = createContext<BookingContextValue | null>(null);
export function BookingProvider({ children }: PropsWithChildren) { const [selectedDoctor, setDoctor] = useState<Doctor>(); const [selectedDate, setDate] = useState<string>(); const [selectedSlot, setSlot] = useState<TimeSlot>(); const [patient, setPatient] = useState<PatientDetails>(); const [paymentMode, setPaymentMode] = useState<PaymentMode>(); const [booking, setBooking] = useState<Booking>(); const reset = () => { setDoctor(undefined); setDate(undefined); setSlot(undefined); setPatient(undefined); setPaymentMode(undefined); setBooking(undefined); }; const confirmBooking = () => { if (!selectedDoctor || !selectedDate || !selectedSlot || !patient || !paymentMode) return; setBooking({ id: `HV-${Date.now().toString().slice(-6)}`, doctor: selectedDoctor, patient, date: selectedDate, slot: selectedSlot, paymentMode, consultationFee: selectedDoctor.fee, platformFee: 50, total: selectedDoctor.fee + 50 }); }; return <BookingContext.Provider value={{ selectedDoctor, selectedDate, selectedSlot, patient, paymentMode, booking, chooseDoctor: setDoctor, chooseSlot: (date, slot) => { setDate(date); setSlot(slot); }, savePatient: setPatient, choosePayment: setPaymentMode, confirmBooking, reset }}>{children}</BookingContext.Provider>; }
export const useBooking = () => { const context = useContext(BookingContext); if (!context) throw new Error('useBooking must be used within BookingProvider'); return context; };